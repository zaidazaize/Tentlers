<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--for text items that describe a specific entry-->
    <style name="TextAppearance.DescriptionTextStyle" parent="TextAppearance.MaterialComponents.Subtitle2">

    </style>

    <!-- Style of room no shown in circle type-->
    <style name="TextAppearance.WhiteTextStyle" parent="TextAppearance.MaterialComponents.Headline5">
        <item name="android:textColor">@color/color_house_name</item>
    </style>

    <!-- for textview describing empty fields-->
    <style name="TextAppearance.EmptyStyle" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textColor">@android:color/secondary_text_dark</item>
    </style>


</resources>